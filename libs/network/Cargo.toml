[package]
name = "network"
version = "0.1.0"
edition = "2021"

[lib]
name = "network"
path = "src/lib.rs"

[dependencies]
model = { workspace = true }
primitives = { workspace = true }
tracing = { workspace = true }
